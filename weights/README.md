# GPT-2 small weights

Place the GPT-2 small checkpoint at:

```text
weights/gpt2-small.pth
```

The generation example uses this repository-local path by default. The
checkpoint is approximately 670 MB and is intentionally ignored by Git.
